package com.sist.web;
import org.springframework.web.bind.annotation.RestController;

@RestController
// id가 없는 상태 => 클래스명이 아이디다 (첫자만 소문자) => movieController
public class MovieController {
  // JSON => AJAX , VUEJS 
}
